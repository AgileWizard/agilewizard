using System.Web.Mvc;
using Xunit;

namespace AgileWizard.Website.Tests
{
    public class LinkShowHideByAuthenticationHelperTest
    {
        [Fact(Skip = "Ignored")]
        public void hide_link_if_not_authenticated()
        {
            //var linkText = "hello world";
            ////var output = LinkShowHideByAuthenticationHelper.AuthenticatedActionLinkFor(linkText);
            //var expected = MvcHtmlString.Create(string.Format(@"<%if (FormsService.IsAuthenticated()) {%>\n<p>\n{0}\n</p>\n<%}%>", linkText));
            //Assert.Equal(expected, output);
        }
    }
}
